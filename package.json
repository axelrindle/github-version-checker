{
    "private": true,
    "name": "github-version-checker",
    "workspaces": [
        "packages/*"
    ],
    "devDependencies": {
        "lerna": "^6.4.1"
    },
    "scripts": {
        "build": "lerna run build",
        "test": "lerna run test"
    }
}
